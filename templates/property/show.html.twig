{% extends 'base.html.twig' %}

{% block title %}{{ property.id }}{% endblock %}

{% block body %}
    <img src="{{ asset('uploads/images/' ~ property.imagePath) }}" width="256">
    <p>Property ID: {{ property.id }}</p>
    <p>Address: {{ property.address }}</p>
    <p>City: {{ property.city }}</p>
    <p>Zip Code: {{ property.zipcode }}</p>
    <p>Country: {{ property.country }}</p>
    <p>Description:<br>{{ property.description }}</p>
    <p>Feature:<br>{{ property.features }}</p>
    <p>Build Year: {{ property.buildyear }}</p>
    <p>Bedrooms: {{ property.bedrooms }}</p>
    <p>Bathrooms: {{ property.bathrooms }}</p>
    <p>Interior: {{ property.sqft }}</p>
    <p>Exterior: {{ property.acres }}</p>
    <p>Property Type: {{ property.type }}</p>
    <p>Agent: {{ property.agent.name }}</p>
    <pre>{{ dump(value) }}</pre>

<hr>
    <h1>Contact the agent</h1>
    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.email) }}
    {{ form_row(form.phone) }}
    {{ form_row(form.message) }}
    <button type="submit">Send Message</button>
    {{ form_end(form) }}
{% endblock %}
